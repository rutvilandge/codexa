import Workspace from "../components/workspace/Workspace";

export default function WorkspaceLayout() {
  return (
    <div className="h-screen w-screen overflow-hidden bg-[#0b0b0c] text-white">
      <Workspace />
    </div>
  );
}